<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Terjadi Kesalahan</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100 flex items-center justify-center h-screen">
    <div class="text-center">
        <h1 class="text-6xl font-bold text-red-500">Error</h1>
        <p class="text-xl text-gray-800 mt-4">Terjadi kesalahan pada server.</p>
        <% if (typeof message !== 'undefined' && message) { %>
            <p class="text-md text-gray-600 mt-2 p-4 bg-red-100 rounded-md"><%= message %></p>
        <% } %>
        <a href="javascript:history.back()" class="mt-6 inline-block bg-blue-500 text-white font-semibold px-6 py-2 rounded-md hover:bg-blue-600">Kembali</a>
    </div>
</body>
</html> 