<<<<<<< HEAD
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body class="flex bg-[#E7EEF0]">
    <%- include('../layout/sidebardospem', { activeMenu: 'mahasiswa', namaDosen: namaDosen }) %>

    <main class="flex-1 p-8 ml-64">
        <div class="flex items-center gap-4 mb-6">
            <a href="/dospem/mahasiswa" class="flex items-center gap-2 px-4 py-2 text-sm bg-white border border-gray-300 text-gray-700 rounded-lg hover:bg-gray-100 font-semibold transition-colors shadow-sm">
                <i data-lucide="arrow-left" class="w-5 h-5 text-gray-700"></i>
                Kembali
            </a>
            <h1 class="text-2xl font-bold text-gray-800">Detail Mahasiswa: <%= mahasiswa.nama %></h1>
        </div>

        <div class="bg-white p-6 rounded-lg shadow-md mb-8">
            <h2 class="text-xl font-semibold text-gray-700 mb-4">Informasi Pribadi</h2>
            <div class="grid grid-cols-1 md:grid-cols-2 gap-4 text-gray-800">
                <div><strong>NIM:</strong> <%= mahasiswa.nim %></div>
                <div><strong>Nama:</strong> <%= mahasiswa.nama %></div>
                <div><strong>Prodi:</strong> <%= mahasiswa.prodi %></div>
                <div><strong>Email:</strong> <%= mahasiswa.email %></div>
                <div><strong>Angkatan:</strong> <%= mahasiswa.angkatan %></div>
            </div>
        </div>

        <div class="bg-white p-6 rounded-lg shadow-md mb-8">
            <h2 class="text-xl font-semibold text-gray-700 mb-4">Status Magang</h2>
            <div class="grid grid-cols-1 md:grid-cols-2 gap-4 text-gray-800">
                <div><strong>Status Magang:</strong> 
                    <span class="font-bold <%= mahasiswa.statusMagang.toLowerCase().includes('disetujui') || mahasiswa.statusMagang.toLowerCase().includes('diterima') ? 'text-green-600' : (mahasiswa.statusMagang.toLowerCase().includes('ditolak') ? 'text-red-600' : 'text-yellow-600') %>">
                        <%= mahasiswa.statusMagang.toUpperCase() %>
                    </span>
                </div>
                <div><strong>Perusahaan Tujuan:</strong> <%= mahasiswa.perusahaanTujuan %></div>
                <div><strong>Periode Magang:</strong> <%= mahasiswa.periodeMagang %></div>
                <div><strong>Logbook Pending:</strong> <%= mahasiswa.logbookPending %></div>
                <div><strong>Status Laporan Akhir:</strong> 
                    <span class="font-bold <%= mahasiswa.statusLaporanAkhir.toLowerCase().includes('menunggu') ? 'text-yellow-600' : (mahasiswa.statusLaporanAkhir.toLowerCase().includes('sudah') ? 'text-green-600' : 'text-gray-600') %>">
                        <%= mahasiswa.statusLaporanAkhir.toUpperCase() %>
                    </span>
                </div>
            </div>
        </div>

        <div class="bg-white p-6 rounded-lg shadow-md mb-8">
            <h2 class="text-xl font-semibold text-gray-700 mb-4">Aksi</h2>
            <div class="flex flex-wrap gap-4">
                <a href="/dospem/evaluasi-logbook" class="inline-block px-4 py-2 bg-blue-600 text-white rounded-md hover:bg-blue-700 transition-colors">
                    Evaluasi Logbook
                </a>
                <a href="/dospem/penilaian-laporan-akhir" class="inline-block px-4 py-2 bg-green-600 text-white rounded-md hover:bg-green-700 transition-colors">
                    Penilaian Laporan Akhir
                </a>
            </div>
        </div>
    </main>

    <!-- Lucide Icons -->
    <script src="https://cdn.jsdelivr.net/npm/lucide@0.271.0/dist/umd/lucide.min.js"></script>
    <script>
      document.addEventListener('DOMContentLoaded', function () {
        if (typeof lucide !== 'undefined') {
          lucide.createIcons();
        } else {
          console.error('Lucide tidak tersedia.');
        }
      });
    </script>
</body>
</html> 
=======
    <!DOCTYPE html>
    <html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title><%= title %></title>
        <link rel="stylesheet" href="/css/style.css">
    </head>
    <body class="flex bg-[#E7EEF0]">
        <%- include('../layout/sidebardospem', { activeMenu: 'mahasiswa', namaDosen: namaDosen }) %>

        <main class="flex-1 p-8 ml-64">
        <h1 class="text-2xl font-bold text-gray-800 mb-6">Detail Mahasiswa: <%= mahasiswa.nama %></h1>

        <div class="flex flex-col md:flex-row gap-8">

            <div class="bg-white p-6 rounded-lg shadow-md mb-8 w-full md:w-1/2">
                <h2 class="text-xl font-semibold text-gray-700 mb-4">Informasi Pribadi</h2>
                <div class="grid grid-cols-1 md:grid-cols-2 gap-4 text-gray-800">
                    <div><strong>NIM:</strong> <%= mahasiswa.nim %></div><br>
                    <div><strong>Nama:</strong> <%= mahasiswa.nama %></div><br>
                    <div><strong>Prodi:</strong> <%= mahasiswa.prodi %></div><br>
                    <div><strong>Email:</strong> <%= mahasiswa.email %></div><br>
                    <div><strong>Angkatan:</strong> <%= mahasiswa.angkatan %></div><br>
                    <div><strong>Telepon:</strong> <%= mahasiswa.no_hp %></div><br>
                    </div>
            </div>

            <div class="bg-white p-6 rounded-lg shadow-md mb-8 w-full md:w-1/2">
                <h2 class="text-xl font-semibold text-gray-700 mb-4">Pengajuan Magang</h2>
                <% if (pengajuanMagang && pengajuanMagang.length > 0) { %>
                    <div class="space-y-4">
                        <% pengajuanMagang.forEach(pengajuan => { %>
                            <div class="border-b border-gray-200 pb-4 last:border-b-0">
                                <p><strong>Perusahaan:</strong> <%= pengajuan.namaPerusahaan %></p>
                                <p><strong>Posisi:</strong> <%= pengajuan.posisi %></p>
                                <p><strong>Periode:</strong> <%= new Date(pengajuan.tanggalMulai).toLocaleDateString('id-ID') %> - <%= new Date(pengajuan.tanggalSelesai).toLocaleDateString('id-ID') %></p>
                                <p><strong>Status:</strong> <span class="font-bold <%= pengajuan.statusPengajuan === 'disetujui' ? 'text-green-600' : (pengajuan.statusPengajuan === 'ditolak' ? 'text-red-600' : 'text-yellow-600') %>"><%= pengajuan.statusPengajuan.toUpperCase() %></span></p>
                                <% if (pengajuan.komentarDosen) { %>
                                    <p><strong>Komentar Dosen:</strong> <%= pengajuan.komentarDosen %></p>
                                <% } %>
                                <div class="mt-2">  
                                    <a href="/docs/surat_budi.pdf" class="inline-block px-3 py-1 text-sm bg-blue-500 text-white rounded-md hover:bg-blue-600">Lihat surat</a>
                                    <a href="/docs/cv_budi.pdf" class="inline-block px-3 py-1 text-sm bg-blue-500 text-white rounded-md hover:bg-blue-600">Lihat cv</a>
                                    <a href="/docs/proposal_budi.pdf" class="inline-block px-3 py-1 text-sm bg-blue-500 text-white rounded-md hover:bg-blue-600">Lihat proposal</a>
                                </div>
                            </div>
                        <% }) %>
                    </div>
                <% } else { %>
                    <p class="text-gray-600">Belum ada pengajuan magang dari mahasiswa ini.</p>
                <% } %>
            </div>

        </div>
            <div class="mt-6">
                <a href="/dospem/detailMahasiswa" class="inline-flex items-center px-4 py-2 border border-gray-300 rounded-md shadow-sm text-sm font-medium text-gray-700 bg-white hover:bg-gray-50 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-indigo-500">
                    &larr; Kembali ke Daftar Mahasiswa
                </a>
            </div>
        </main>
    </body>
    </html>
>>>>>>> 7e7ac080241c89bb016f2879b7eaf063e8d85df5
